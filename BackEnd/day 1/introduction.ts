/** @format */
"use strict";

let x: number = 10;
// coba kalo array bisa pake ini
// const array: number[] = [1,2,3,4,5,6]
let y: number = 2;

function add(x: number, y: number): number {
  return x + y;
}

let result = add(x, y);
console.log(result);
